# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=4PEtD.DxSLOp619n&X}3~yAPYhg[J3j4wYqX!,b]E8.bm(qVx&
POSTGRES_DB=access_control_service
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Frontend
NODE_ENV=production
FRONTEND_PORT=3000
API_BASE_URL=https://your-domain.com

## Production
DOMAIN=yourdomain.com
CERTBOT_EMAIL=your@email.com

# Backend
HOST=0.0.0.0
PORT=8000
EXPIRES_IN=10800

## Enable auto-reload in development
RELOAD=false

## Logging
LOG_LEVEL=warning
ACCESS_LOG=false

## Workers (recommended for production)
WORKERS=4

## Lifespan handling
LIFESPAN=auto

## HTTP protocol implementation
HTTP=auto

## WebSocket implementation
WS=auto

## Proxy headers (if behind nginx / load balancer)
PROXY_HEADERS=true
FORWARDED_ALLOW_IPS=127.0.0.1

## Limit max requests before restart (helps memory leaks)
LIMIT_MAX_REQUESTS=10000